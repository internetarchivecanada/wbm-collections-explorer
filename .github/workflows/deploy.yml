name: Build and deploy the serverless edition

on:
  push:
    branches: [main]
  schedule:
    # 06:45 UTC, matching the studio deployment's nightly refresh
    - cron: '45 6 * * *'
  workflow_dispatch:
    inputs:
      refresh:
        description: Re-harvest from web.archive.org before building
        type: boolean
        default: true

permissions:
  contents: read
  pages: write
  id-token: write

# Never let two deploys race; let a running one finish.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - run: pip install -r requirements-build.txt

      - id: pages
        uses: actions/configure-pages@v5

      - name: Test
        run: python test_refresh.py

      # Best-effort, and deliberately slower than the studio's --sleep 2:
      # web.archive.org refuses a share of these requests from runner IPs (12 of
      # 49 on the first run). Whatever fails, refresh.py carries the previous
      # profile forward from the committed dataset, so a bad harvest costs
      # freshness and never data.
      - name: Re-harvest from the Wayback Machine
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' && inputs.refresh
        continue-on-error: true
        run: python app/refresh.py --sleep 5

      - name: Build the static site
        run: |
          python build.py \
            --base "${{ steps.pages.outputs.base_path }}/" \
            --site-url "${{ steps.pages.outputs.base_url }}"

      - name: Check the build
        run: python check_build.py dist

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
